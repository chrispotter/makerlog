# Session secret for the API service
# Generate a strong secret with: openssl rand -base64 32
SESSION_SECRET=your-secret-key-change-this-in-production

# Other environment variables can be added here as needed
